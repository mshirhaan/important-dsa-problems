function dfs(root) {
    let stack1 = [root];
    let result = [];

    while(stack1.length) {
        let node = stack1.pop();
        result.push(node.val);
        if(node.right) {
            stack1.push(node.right);
        }
        if(node.left) {
            stack1.push(node.left);
        }
    }
    return result;
}
