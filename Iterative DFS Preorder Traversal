function dfs(root) {
    let stack1 = [root];
    let stack2 = [];
    let result = [];

    while(stack1.length || stack2.length) {
        let node = stack.pop();
        stack2.push(node);
        if(node.right) {
            stack.push(node.right);
        }
        result.push(node.val);
        if(node.left) {
            stack.push(node.left);
        }
    }
    return result;
}
